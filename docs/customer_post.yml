Insert a Customer
Definitions to how use api to insert a customer.
---
tags:
  - Customer
parameters:
  - name: body
    in: body
    required: true
    schema:
      email: string
      name: string
      required:
        - email
        - name
        - token
      properties:
        email:
          type: string
          description: The email of Customer
        name:
          type: string
          description: The name of Customer
        token:
          type: string
          description: Token to auth in app